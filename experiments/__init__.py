"""Experiment entry points.

Scripts here are run as modules (``python -m experiments.calibrate``) so that relative
imports resolve and the repository root does not have to be on the path by accident.
"""
